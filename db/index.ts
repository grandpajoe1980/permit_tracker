import * as schema from "./schema";

/**
 * PATH Relational Schema Export
 * Supabase PostgreSQL is the authoritative production database runtime for PATH.
 * The schema definitions below export the relational domain topology.
 */
export { schema };
